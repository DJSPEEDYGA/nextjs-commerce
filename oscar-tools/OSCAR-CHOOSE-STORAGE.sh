#!/usr/bin/env bash
# OSCAR-CHOOSE-STORAGE.sh — Interactive drive/storage picker for Oscar.
# Lets you choose which drive or folder to use for each data category
# (LLM models, image models, outputs, cache, logs, downloads, etc.)
# and writes .oscar-storage-config.env so all Oscar scripts respect it.
#
# Usage:
#   bash OSCAR-CHOOSE-STORAGE.sh
#   bash OSCAR-CHOOSE-STORAGE.sh --auto   # auto-detect, no prompts
#   bash OSCAR-CHOOSE-STORAGE.sh --list   # list drives and exit

set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
STAMP="$(date +%Y%m%d-%H%M%S)"

GREEN='\033[0;32m'; YELLOW='\033[1;33m'; CYAN='\033[0;36m'
RED='\033[0;31m'; BOLD='\033[1m'; NC='\033[0m'

say()  { printf '%b\n' "${CYAN}[INFO]${NC} $*"; }
ok()   { printf '%b\n' "${GREEN}[OK]${NC} $*"; }
warn() { printf '%b\n' "${YELLOW}[WARN]${NC} $*"; }
die()  { printf '%b\n' "${RED}[ERROR]${NC} $*" >&2; exit 1; }

# ── Detect mounted volumes ───────────────────────────────────────────
declare -a DRIVE_PATHS=()
declare -a DRIVE_LABELS=()
declare -a DRIVE_FREE_GB=()
declare -a DRIVE_TOTAL_GB=()

detect_drives() {
  local line mount_path free_kb total_kb free_gb total_gb label
  # macOS and Linux standard mount points
  while IFS= read -r line; do
    mount_path="$(echo "$line" | awk '{print $NF}')"
    case "$mount_path" in
      /Volumes/*|/media/*|/run/media/*|/mnt/*) ;;
      /) ;; # include root
      *) continue ;;
    esac
    total_kb="$(echo "$line" | awk '{print $2}')"
    free_kb="$(echo "$line" | awk '{print $4}')"
    [ -z "$free_kb" ] && continue
    [ "$total_kb" -lt 1048576 ] 2>/dev/null && continue  # skip < 1GB
    total_gb=$((total_kb / 1024 / 1024))
    free_gb=$((free_kb / 1024 / 1024))
    label="$(basename "$mount_path")"
    [ "$mount_path" = "/" ] && label="(internal disk)"
    DRIVE_PATHS+=("$mount_path")
    DRIVE_LABELS+=("$label")
    DRIVE_FREE_GB+=("$free_gb")
    DRIVE_TOTAL_GB+=("$total_gb")
  done < <(df -Pk 2>/dev/null | awk 'NR>1')

  # Also check for any custom path provided by the user
  if [ -n "${OSCAR_EXTRA_DRIVE:-}" ] && [ -d "$OSCAR_EXTRA_DRIVE" ]; then
    local ek ef et
    ek="$(df -Pk "$OSCAR_EXTRA_DRIVE" | awk 'NR==2 {print $4}')"
    et="$(df -Pk "$OSCAR_EXTRA_DRIVE" | awk 'NR==2 {print $2}')"
    ef=$((ek / 1024 / 1024))
    et=$((et / 1024 / 1024))
    DRIVE_PATHS+=("$OSCAR_EXTRA_DRIVE")
    DRIVE_LABELS+=("$(basename "$OSCAR_EXTRA_DRIVE")")
    DRIVE_FREE_GB+=("$ef")
    DRIVE_TOTAL_GB+=("$et")
  fi
}

print_drives() {
  local i
  printf '\n%b%-4s %-40s %10s %10s%b\n' "$BOLD" "#" "DRIVE / MOUNT POINT" "FREE" "TOTAL" "$NC"
  printf '%.0s─' {1..70}; printf '\n'
  for i in "${!DRIVE_PATHS[@]}"; do
    printf '  %-2s  %-40s %7s GB %7s GB\n' \
      "$((i + 1))" "${DRIVE_PATHS[$i]}" "${DRIVE_FREE_GB[$i]}" "${DRIVE_TOTAL_GB[$i]}"
  done
  printf '\n'
}

# ── Pick a drive for a category ──────────────────────────────────────
pick_drive() {
  local category="$1"
  local description="$2"
  local default_idx="${3:-1}"
  local subfolder="${4:-}"
  local choice path

  printf '%b▸ %s%b\n' "$BOLD" "$category" "$NC"
  printf '  %s\n' "$description"
  if [ -n "$subfolder" ]; then
    printf '  (subfolder: %s)\n' "$subfolder"
  fi

  while true; do
    read -r -p "  Choose drive number [${default_idx}]: " choice
    choice="${choice:-$default_idx}"
    if [[ "$choice" =~ ^[0-9]+$ ]] && (( choice >= 1 && choice <= ${#DRIVE_PATHS[@]} )); then
      path="${DRIVE_PATHS[$((choice - 1))]}"
      if [ -n "$subfolder" ]; then
        path="$path/$subfolder"
      fi
      printf '  → %s\n\n' "$path"
      echo "$path"
      return 0
    elif [ "$choice" = "c" ] || [ "$choice" = "C" ]; then
      read -r -p "  Enter custom path: " path
      if [ -n "$path" ]; then
        printf '  → %s\n\n' "$path"
        echo "$path"
        return 0
      fi
    fi
    warn "Invalid choice. Enter a number (1-${#DRIVE_PATHS[@]}) or 'c' for custom path."
  done
}

# ── Write the config ─────────────────────────────────────────────────
write_config() {
  local out="$1"
  cat > "$out" <<ENVFILE
# Oscar Multi-Drive Storage Configuration
# Generated by OSCAR-CHOOSE-STORAGE.sh on $(date)
# Source this file to route Oscar data across your drives.
#
# To regenerate: bash OSCAR-CHOOSE-STORAGE.sh
# To override a single category: export OSCAR_DRIVE_xxx=/your/path before running scripts.

# ── Primary Oscar home (app files, tools, crew data) ─────────────
export OSCAR_DRIVE_APP="$CHOSEN_APP"

# ── LLM model storage (Ollama models — typically the largest) ────
export OSCAR_DRIVE_LLM_MODELS="$CHOSEN_LLM"

# ── GGUF model files (Hugging Face GGUF picks) ──────────────────
export OSCAR_DRIVE_GGUF="$CHOSEN_GGUF"

# ── Image generation models (checkpoints, flux, loras, vae …) ───
export OSCAR_DRIVE_IMAGE_MODELS="$CHOSEN_IMAGE_MODELS"

# ── Generated outputs (images, renders, exports) ────────────────
export OSCAR_DRIVE_OUTPUTS="$CHOSEN_OUTPUTS"

# ── Downloads (general download targets) ────────────────────────
export OSCAR_DRIVE_DOWNLOADS="$CHOSEN_DOWNLOADS"

# ── Cache (HF cache, pip, torch, xdg) ──────────────────────────
export OSCAR_DRIVE_CACHE="$CHOSEN_CACHE"

# ── Logs ────────────────────────────────────────────────────────
export OSCAR_DRIVE_LOGS="$CHOSEN_LOGS"

# ── Runtime (venvs, temp, pid files, Ollama runtime) ────────────
export OSCAR_DRIVE_RUNTIME="$CHOSEN_RUNTIME"

# ── GOAT app data ──────────────────────────────────────────────
export OSCAR_DRIVE_GOAT_DATA="$CHOSEN_GOAT"

# ── Backward compatibility aliases ──────────────────────────────
export FKD1_ROOT="\$OSCAR_DRIVE_APP"
export OSCAR_HOME="\$OSCAR_DRIVE_APP"
export OLLAMA_MODELS="\$OSCAR_DRIVE_LLM_MODELS"
export OSCAR_MODEL_STORE="\$OSCAR_DRIVE_LLM_MODELS"
export OSCAR_GGUF_STORE="\$OSCAR_DRIVE_GGUF"
export GOAT_DATA_ROOT="\$OSCAR_DRIVE_GOAT_DATA"
export GOAT_DOWNLOAD_DIR="\$OSCAR_DRIVE_DOWNLOADS"
ENVFILE
}

# ── Also update .oscar-one-folder.env if it exists ───────────────────
update_one_folder_env() {
  local root="$CHOSEN_APP"
  local envfile="$root/.oscar-one-folder.env"
  [ -d "$root" ] || mkdir -p "$root"

  cat > "$envfile" <<'HEADER'
# Oscar One-Folder Environment (multi-drive aware)
# Auto-generated by OSCAR-CHOOSE-STORAGE.sh
HEADER

  cat >> "$envfile" <<ENVBODY
export OSCAR_HOME="$CHOSEN_APP"
export FKD1_ROOT="$CHOSEN_APP"
export OSCAR_BRIDGE_WORKSPACE="$CHOSEN_APP"
export OSCAR_TOOL_WORKSPACE="$CHOSEN_APP"
export OSCAR_ASSET_ROOT="$CHOSEN_APP"

export OLLAMA_MODELS="$CHOSEN_LLM"
export OSCAR_MODEL_STORE="$CHOSEN_LLM"
export OSCAR_GGUF_STORE="$CHOSEN_GGUF"

export GOAT_DATA_ROOT="$CHOSEN_GOAT"
export GOAT_DOWNLOAD_DIR="$CHOSEN_DOWNLOADS"

export OSCAR_OUTPUT_DIR="$CHOSEN_OUTPUTS"
export OSCAR_IMAGE_OUTPUT_DIR="$CHOSEN_OUTPUTS/images"
export OSCAR_IMAGE_MODEL_ROOT="$CHOSEN_IMAGE_MODELS"

export OSCAR_LOG_DIR="$CHOSEN_LOGS"
export OSCAR_IMAGE_LOG_DIR="$CHOSEN_LOGS"

export OSCAR_RUNTIME_DIR="$CHOSEN_RUNTIME"
export OSCAR_RUNTIME_HOME="$CHOSEN_RUNTIME"
export OLLAMA_HOME="$CHOSEN_RUNTIME"
export OLLAMA_RUNNERS_DIR="$CHOSEN_RUNTIME/runners"
export OLLAMA_TMPDIR="$CHOSEN_RUNTIME/tmp"
export TMPDIR="$CHOSEN_CACHE/tmp"

export HF_HOME="$CHOSEN_CACHE/huggingface"
export TRANSFORMERS_CACHE="$CHOSEN_CACHE/huggingface/transformers"
export HUGGINGFACE_HUB_CACHE="$CHOSEN_CACHE/huggingface/hub"
export XDG_CACHE_HOME="$CHOSEN_CACHE/xdg"
export TORCH_HOME="$CHOSEN_CACHE/torch"
export PIP_CACHE_DIR="$CHOSEN_CACHE/pip"

export OLLAMA_HOST="\${OLLAMA_HOST:-127.0.0.1:11434}"
export OLLAMA_CONTEXT_LENGTH="\${OLLAMA_CONTEXT_LENGTH:-32768}"
export OLLAMA_NUM_PARALLEL="\${OLLAMA_NUM_PARALLEL:-1}"
export OLLAMA_MAX_LOADED_MODELS="\${OLLAMA_MAX_LOADED_MODELS:-1}"

export OSCAR_IMAGE_RENDER_ENDPOINT="\${OSCAR_IMAGE_RENDER_ENDPOINT:-http://127.0.0.1:3344/api/draw}"
export GOAT_IMAGE_RENDER_ENDPOINT="\${GOAT_IMAGE_RENDER_ENDPOINT:-\$OSCAR_IMAGE_RENDER_ENDPOINT}"
export OSCAR_DRAW_PORT="\${OSCAR_DRAW_PORT:-3344}"
export OSCAR_CHAT_PORT="\${OSCAR_CHAT_PORT:-3333}"
export GOAT_WEB_PORT="\${GOAT_WEB_PORT:-8765}"
export GOAT_INTEL_PORT="\${GOAT_INTEL_PORT:-5500}"

export OSCAR_ALLOW_ONLINE="\${OSCAR_ALLOW_ONLINE:-0}"
export OSCAR_OFFLINE="\${OSCAR_OFFLINE:-1}"
export HF_HUB_OFFLINE="\$OSCAR_OFFLINE"
export TRANSFORMERS_OFFLINE="\$OSCAR_OFFLINE"
ENVBODY

  ok "Updated: $envfile"
}

# ── Also update oscar-image-runtimes.env if applicable ───────────────
update_image_runtimes_env() {
  local root="$CHOSEN_APP"
  local envfile="$root/oscar-image-runtimes.env"
  local runtime_root="$root/image-runtimes"

  cat > "$envfile" <<ENVBODY
export OSCAR_HOME="$CHOSEN_APP"
export OSCAR_IMAGE_RUNTIME_ROOT="$runtime_root"
export OSCAR_IMAGE_MODEL_ROOT="$CHOSEN_IMAGE_MODELS"
export OSCAR_IMAGE_OUTPUT_DIR="$CHOSEN_OUTPUTS/images"
export OSCAR_IMAGE_LOG_DIR="$CHOSEN_LOGS"
export OSCAR_IMAGE_PYTHON="\${OSCAR_IMAGE_PYTHON:-\$(command -v python3 2>/dev/null || command -v python 2>/dev/null || echo python3)}"
export HF_HOME="$CHOSEN_CACHE/huggingface"
export TRANSFORMERS_CACHE="$CHOSEN_CACHE/huggingface/transformers"
export HUGGINGFACE_HUB_CACHE="$CHOSEN_CACHE/huggingface/hub"
export XDG_CACHE_HOME="$CHOSEN_CACHE/xdg"
export TORCH_HOME="$CHOSEN_CACHE/torch"
export PIP_CACHE_DIR="$CHOSEN_CACHE/pip"
export COMFYUI_DIR="$runtime_root/ComfyUI"
export A1111_DIR="$runtime_root/stable-diffusion-webui"
export FORGE_DIR="$runtime_root/stable-diffusion-webui-forge"
export INVOKEAI_DIR="$runtime_root/InvokeAI"
export DIFFUSERS_DIR="$runtime_root/diffusers"
export OSCAR_COMFY_PORT="\${OSCAR_COMFY_PORT:-8188}"
export OSCAR_A1111_PORT="\${OSCAR_A1111_PORT:-7860}"
export OSCAR_FORGE_PORT="\${OSCAR_FORGE_PORT:-7861}"
export OSCAR_INVOKEAI_PORT="\${OSCAR_INVOKEAI_PORT:-9090}"
ENVBODY

  ok "Updated: $envfile"
}

# ── Create required directories ──────────────────────────────────────
create_directories() {
  say "Creating storage directories..."
  local d
  for d in \
    "$CHOSEN_APP" \
    "$CHOSEN_LLM" \
    "$CHOSEN_GGUF" \
    "$CHOSEN_IMAGE_MODELS" \
    "$CHOSEN_IMAGE_MODELS/checkpoints" \
    "$CHOSEN_IMAGE_MODELS/flux" \
    "$CHOSEN_IMAGE_MODELS/loras" \
    "$CHOSEN_IMAGE_MODELS/vae" \
    "$CHOSEN_IMAGE_MODELS/vae_approx" \
    "$CHOSEN_IMAGE_MODELS/controlnet" \
    "$CHOSEN_IMAGE_MODELS/embeddings" \
    "$CHOSEN_IMAGE_MODELS/upscalers" \
    "$CHOSEN_IMAGE_MODELS/clip" \
    "$CHOSEN_IMAGE_MODELS/clip_vision" \
    "$CHOSEN_IMAGE_MODELS/unet" \
    "$CHOSEN_IMAGE_MODELS/text_encoders" \
    "$CHOSEN_IMAGE_MODELS/ipadapter" \
    "$CHOSEN_IMAGE_MODELS/diffusers" \
    "$CHOSEN_IMAGE_MODELS/invokeai" \
    "$CHOSEN_OUTPUTS" \
    "$CHOSEN_OUTPUTS/images" \
    "$CHOSEN_DOWNLOADS" \
    "$CHOSEN_CACHE" \
    "$CHOSEN_CACHE/huggingface" \
    "$CHOSEN_CACHE/pip" \
    "$CHOSEN_CACHE/torch" \
    "$CHOSEN_CACHE/xdg" \
    "$CHOSEN_CACHE/tmp" \
    "$CHOSEN_LOGS" \
    "$CHOSEN_RUNTIME" \
    "$CHOSEN_RUNTIME/pids" \
    "$CHOSEN_RUNTIME/runners" \
    "$CHOSEN_RUNTIME/tmp" \
    "$CHOSEN_GOAT"; do
    mkdir -p "$d" 2>/dev/null || warn "Could not create: $d"
  done
  ok "Storage directories created."
}

# ── Print summary ────────────────────────────────────────────────────
print_summary() {
  printf '\n%b════════════════════════════════════════════════════════════%b\n' "$BOLD" "$NC"
  printf '%b  OSCAR STORAGE CONFIGURATION SUMMARY%b\n' "$BOLD" "$NC"
  printf '%b════════════════════════════════════════════════════════════%b\n' "$BOLD" "$NC"
  printf '  App / Home:        %s\n' "$CHOSEN_APP"
  printf '  LLM Models:        %s\n' "$CHOSEN_LLM"
  printf '  GGUF Models:       %s\n' "$CHOSEN_GGUF"
  printf '  Image Models:      %s\n' "$CHOSEN_IMAGE_MODELS"
  printf '  Outputs:           %s\n' "$CHOSEN_OUTPUTS"
  printf '  Downloads:         %s\n' "$CHOSEN_DOWNLOADS"
  printf '  Cache:             %s\n' "$CHOSEN_CACHE"
  printf '  Logs:              %s\n' "$CHOSEN_LOGS"
  printf '  Runtime:           %s\n' "$CHOSEN_RUNTIME"
  printf '  GOAT Data:         %s\n' "$CHOSEN_GOAT"
  printf '%b════════════════════════════════════════════════════════════%b\n\n' "$BOLD" "$NC"
}

# ── Main ─────────────────────────────────────────────────────────────
main() {
  printf '\n%b╔══════════════════════════════════════════════════════════╗%b\n' "$BOLD" "$NC"
  printf '%b║        OSCAR MULTI-DRIVE STORAGE CONFIGURATOR           ║%b\n' "$BOLD" "$NC"
  printf '%b╚══════════════════════════════════════════════════════════╝%b\n' "$BOLD" "$NC"
  printf '\nThis tool lets you choose which drive or folder to use for\n'
  printf 'each type of Oscar data. Large files (LLM models, image models)\n'
  printf 'can go on big/fast drives; small files (logs, cache) can stay local.\n'

  detect_drives

  if [ ${#DRIVE_PATHS[@]} -eq 0 ]; then
    die "No suitable drives detected. Mount a drive or set OSCAR_EXTRA_DRIVE=/path."
  fi

  if [ "${1:-}" = "--list" ]; then
    print_drives
    exit 0
  fi

  print_drives
  printf 'Enter a drive number for each category, or %bc%b for a custom path.\n\n' "$BOLD" "$NC"

  # Find a good default (prefer FKD1 if present)
  local default_idx=1
  local i
  for i in "${!DRIVE_LABELS[@]}"; do
    if [ "${DRIVE_LABELS[$i]}" = "FKD1" ]; then
      default_idx=$((i + 1))
      break
    fi
  done

  CHOSEN_APP="$(pick_drive \
    "Oscar App / Home" \
    "Main Oscar folder: tools, crew data, scripts, web app." \
    "$default_idx" \
    "OSCAR-THOR-ONE-FOLDER")"

  CHOSEN_LLM="$(pick_drive \
    "LLM Models (Ollama)" \
    "Ollama model blobs. 29-model pack is ~400GB. Put on your biggest/fastest drive." \
    "$default_idx" \
    "models/ollama")"

  CHOSEN_GGUF="$(pick_drive \
    "GGUF Models" \
    "Hugging Face GGUF files. Can be tens of GB." \
    "$default_idx" \
    "models/gguf")"

  CHOSEN_IMAGE_MODELS="$(pick_drive \
    "Image Models" \
    "Checkpoints, FLUX, LoRA, VAE, ControlNet, embeddings, upscalers." \
    "$default_idx" \
    "models/image")"

  CHOSEN_OUTPUTS="$(pick_drive \
    "Outputs" \
    "Generated images, renders, exports." \
    "$default_idx" \
    "outputs")"

  CHOSEN_DOWNLOADS="$(pick_drive \
    "Downloads" \
    "General download targets." \
    "$default_idx" \
    "downloads")"

  CHOSEN_CACHE="$(pick_drive \
    "Cache" \
    "HF cache, pip, torch, xdg — can grow large with model downloads." \
    "$default_idx" \
    "cache")"

  CHOSEN_LOGS="$(pick_drive \
    "Logs" \
    "Oscar/GOAT service logs." \
    "$default_idx" \
    "logs")"

  CHOSEN_RUNTIME="$(pick_drive \
    "Runtime" \
    "Venvs, Ollama runtime, temp files, PID files." \
    "$default_idx" \
    "runtime")"

  CHOSEN_GOAT="$(pick_drive \
    "GOAT App Data" \
    "GOAT Royalty App data files." \
    "$default_idx" \
    "data")"

  print_summary

  # Write configs
  local config_out="$CHOSEN_APP/.oscar-storage-config.env"
  write_config "$config_out"
  ok "Storage config saved: $config_out"

  # Also write a copy next to this script
  if [ "$SCRIPT_DIR" != "$CHOSEN_APP" ]; then
    write_config "$SCRIPT_DIR/.oscar-storage-config.env"
    ok "Storage config copy: $SCRIPT_DIR/.oscar-storage-config.env"
  fi

  create_directories
  update_one_folder_env
  update_image_runtimes_env

  printf '\n%b DONE! %b Your Oscar scripts will now use the drives you chose.\n' "$GREEN" "$NC"
  printf 'Config file: %s\n' "$config_out"
  printf '\nTo reconfigure later, run:\n'
  printf '  bash %s/OSCAR-CHOOSE-STORAGE.sh\n' "$SCRIPT_DIR"
  printf '\nTo test your setup:\n'
  printf '  bash %s/TEST-OSCAR-ONE-FOLDER.sh\n' "$CHOSEN_APP"
  printf '\n'
}

main "$@"
